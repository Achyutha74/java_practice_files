class Solution {
    public boolean isPalindrome(int x) {
     int a=x;
     int sum=0,rem;
     if(x<0)
     
        return false;
     
    
     while(x!=0)
     {
        rem=x%10;
        sum=sum*10+rem;
        x=x/10;
     }
     
     return sum==a;
    }
    
}
